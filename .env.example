# Server-side Buttondown API key. Used by app/api/subscribe to subscribe emails.
BUTTONDOWN_API_KEY=
  
# Public form endpoint the signup form POSTs to.
# Set to /api/subscribe to use the built-in proxy (recommended).
# Leave empty to stub the form (shows success without a network request).
NEXT_PUBLIC_BUTTONDOWN_ENDPOINT=